# Assets

Сюда пользователь кладёт экспорт из макета Coffee House. Агент файл не скачал: у Figma MCP нет edit access, повторный импорт не делался. Список и пути: [docs/specs/overview.md](../docs/specs/overview.md). Заглушки не добавлять.

```text
assets/
  images/
  icons/
  fonts/
  favicon.svg   # или .ico / .png
```
